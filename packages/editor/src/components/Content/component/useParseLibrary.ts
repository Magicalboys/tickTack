// import { LibraryComponentInstanceData } from "@tickTack/types/src/library-component";
// import { libraryMap } from "@tickTack/library";

// /**
//  * 解析画布渲染组件的钩子
//  * @returns
//  */
// export default function useParseLibrary() {
//   const parseLibraryComponent = (data: LibraryComponentInstanceData) => {
//     const component = libraryMap[data.componentName];

//     if (!component) {
//       throw new Error(`library component: ${data.libraryName} not found`);
//     }
//   };
//   return {
//     parseLibraryComponent,
//   };
// }
